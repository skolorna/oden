/**
 * A regular expression matching `http(s)://localhost:<OPTIONAL PORT>`.
 */
export const LOCALHOST_HTTP_REGEX = /^https?:\/\/localhost(?::[0-9]+)?$/;

export const SKOLORNA_HTTP_REGEX = /^https?:\/\/(?:.+\.)?skolorna\.com$/;
